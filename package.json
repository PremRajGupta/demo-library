{
  "name": "demo-library-root",
  "version": "1.0.0",
  "description": "Root workspace for Smart Library Management System",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix app",
    "build": "npm install --prefix app && npm run build --prefix app",
    "dev:server": "npm run dev --prefix server",
    "dev:app": "npm run dev --prefix app",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:app\"",
    "test:server": "npm test --prefix server",
    "test:app": "npm test --prefix app",
    "test:all": "npm run test:server && npm run test:app"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
